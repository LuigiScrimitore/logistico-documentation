# guide_dev/ — Guide operative locali (sviluppo)

Guide per l'ambiente di sviluppo locale.

- `Test Locale - Quickstart.md`, `Test Locale - Bronze.md` — esecuzione pipeline in locale
- `manuale-recupero-spazio-docker.md` — recupero spazio disco Docker/WSL (vedi anche memoria wsl-vhdx-disk-reclaim)
