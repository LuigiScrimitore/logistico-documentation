# SAL Logistico 2.0 — Stato del progetto (aggiornamento)

**Data:** 2026-09-04 · **Preparato da:** Team Logistico 2.0 · **Uso:** stato per SAL cliente
**Precedente:** [SAL_2026-08-27](SAL_2026-08-27_stato.md) (Gantt per wave + dettaglio invariati salvo quanto sotto)

---

## 1. Sintesi esecutiva del delta (dal 27/08)
Passo avanti sostanziale sulla **Traccia B (validazione cloud)**: **primo run E2E completo dei 7 job in DEV su
dati reali** (run_date 2026-09-02) portato a **7/7 verde**.

- **Da 1/7 a 7/7 job verdi** in un unico run orchestrato: `landing_ingestion`, `dim_refresh`, `carichi`,
  `giacenze`, `trasporti`, `prep_sped`, `datamart` — zero task falliti.
- **Canonico sito risolto** ([[ADR-0026]]): chiave **numerica** unica su tutte le aree + attributi alfabetici
  (`LGAX`/`0020A`) e descrizione su `LU_SITO`. Orphan sito **100%→0** (F_CARICO, F_TRASPORTO). Chiude
  [[OP-TRA-1]]. Anagrafica autoritativa dei 22 siti = `S_LOGISTIX`+`WL1`.
- **Dead-code CND rimosso** ([[ACT_CND-01]]): 7 notebook + task; giacenze usa `silver.t_stock` (catena),
  trasporti/vettori da TRACK. Chiude [[OP-CND-1]].
- **Classe "ANSI serverless + dati legacy sporchi" risolta** ([[ACT_9027]]/[[LL-027]]/[[LL-028]]):
  `try_cast`/`try_to_timestamp`, `withSchemaEvolution()`, `julian_to_date` ANSI-safe, bronze skip-safe.

## 2. Release pubblicata (2026-09-04)
Ciclo multi-repo eseguito (monorepo → GitHub → GitLab cliente):
- **GitHub** (SoT): `logistico-lib`, `logistico-workflows`, `logistico-documentation` aggiornati;
  `logistico-infrastructure` invariato.
- **GitLab cliente**: `logistico-lib **v1.0.5**` + `logistico-workflows **v0.1.6**` (push ff, history
  preservata); **CI cliente verdi**. `logistico-infrastructure` invariato (`v0.1.6`, non ripubblicato).
Dettaglio in [16_runbook_multirepo_github_gitlab.md](16_runbook_multirepo_github_gitlab.md).

## 3. Stato per traccia
- **Traccia A — build**: completa; consolidata con i fix strutturali del giro (canonico, ANSI, CND).
- **Traccia B — validazione cloud**: **avviata concretamente**. Pipeline E2E gira su serverless con dati reali.
  Prossimi gate: **backfill storico (7 giorni) + test incrementale**, poi **quadratura vs CDT_DW** ([[OP-QDR-1]],
  non significativa senza storico), validazione business, shadow mode, cut-over.

## 4. Prossimi passi
1. Backfill 7 giorni (2026-08-27→09-02) con run **incrementale** giorno-per-giorno (test watermark/volumi).
2. Quadratura fact Gold vs CDT_DW sullo storico.
3. Seed reale `cartellino`/`dettaglio_carr` per fact carrellisti a volume pieno (ora skip-safe).
